{
  "name": "freddybremseth-books",
  "version": "1.0.0",
  "private": true,
  "description": "books.freddybremseth.com — author site (static app + Vercel serverless API).",
  "scripts": {
    "build": "node scripts/build-seo-pages.mjs && node scripts/build-sitemap.mjs && node scripts/audit-catalog.mjs",
    "build:seo": "node scripts/build-seo-pages.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "audit:catalog": "node scripts/audit-catalog.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^16.8.0"
  }
}
