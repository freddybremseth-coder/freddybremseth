(()=>{'use strict';
 const $=id=>document.getElementById(id);
 const state={art:[],styles:[],filtered:[],count:18,category:'all',search:'',sort:'style',selected:null,lang:'en',checkout:false,print:{byArtworkId:{},defaultPrintUrl:''}};
 const copy={en:{all:'All styles',loading:'Loading the gallery…',results:'artworks',more:'View more artworks ↓',buy:'Buy digital edition · €50 ↗',unavailable:'Digital checkout opening soon',agree:'Please accept the digital delivery terms first.',checkoutError:'Checkout is temporarily unavailable. No payment was taken.',pending:'Confirming your payment…',paid:'Payment confirmed. Your download is available below.',failed:'This payment has not yet been confirmed. You have not been charged by this page.',fileMissing:'Your payment was verified, but the file is not yet ready. Please keep the Stripe confirmation link and contact the gallery.',download:'Download artwork ↓'},no:{all:'Alle stiler',loading:'Laster galleriet…',results:'kunstverk',more:'Vis flere kunstverk ↓',buy:'Kjøp digital utgave · €50 ↗',unavailable:'Digital betaling åpner snart',agree:'Godta vilkårene for digital levering først.',checkoutError:'Betaling er midlertidig utilgjengelig. Ingen betaling er gjennomført.',pending:'Kontrollerer betalingen…',paid:'Betalingen er bekreftet. Du kan laste ned kunstverket.',failed:'Betalingen er ennå ikke bekreftet. Du er ikke belastet av denne siden.',fileMissing:'Betalingen er bekreftet, men filen er ennå ikke tilgjengelig. Ta vare på Stripe-lenken og kontakt galleriet.',download:'Last ned kunstverket ↓'}};
 const text=key=>copy[state.lang][key];
 const showToast=s=>{const node=$('toast');node.textContent=s;node.classList.add('on');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>node.classList.remove('on'),4600)};
 const safeLink=x=>{try{const u=new URL(x);return (u.protocol==='https:'||u.protocol==='http:')?u.toString():null}catch{return null}};
 const escapeHtml=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const detailHref=art=>'/verk/'+encodeURIComponent(art.id)+'/';
 const currentSlug=()=>{const m=location.pathname.match(/^\/verk\/([a-z0-9-]+)\/?$/);return m?m[1]:null};
 const byStyle=(a,b)=>state.styles.findIndex(s=>s.id===a.style_id)-state.styles.findIndex(s=>s.id===b.style_id)||a.number-b.number;
 const cards=(arts)=>{const frag=document.createDocumentFragment();for(const art of arts){const card=document.createElement('article');card.className='art-card';const button=document.createElement('button');button.type='button';button.setAttribute('aria-label','View '+art.title);button.innerHTML=`<span class="art-photo"><img loading="lazy" src="${art.thumb}" alt="${escapeHtml(art.title)}, ${escapeHtml(art.category)} digital artwork" width="${art.width}" height="${art.height}"><span class="art-overlay">Explore artwork ↗</span></span><span class="art-card-meta"><span><span class="art-title">${escapeHtml(art.title)}</span><span class="art-category">${escapeHtml(art.category)} / Digital art</span></span><span class="art-number">${String(art.number).padStart(3,'0')}</span></span>`;button.addEventListener('click',()=>openArt(art,true));card.appendChild(button);frag.appendChild(card)}return frag};
 function setStyle(category){if(category!=='all'&&!state.styles.some(s=>s.id===category))return;state.category=category;state.sort='style';$('art-sort').value='style';state.count=18;state.search='';$('art-search').value='';render();$('collection').scrollIntoView({behavior:'smooth',block:'start'})}
 function makeStyles(){const host=$('categories');host.replaceChildren();const all=document.createElement('button');all.className='chip';all.dataset.category='all';all.textContent=`All styles (${state.art.length})`;host.appendChild(all);for(const style of state.styles){const button=document.createElement('button');button.className='chip';button.dataset.category=style.id;button.textContent=`${style.name} (${style.count})`;host.appendChild(button)}}
 function render(){
  const style=state.styles.find(s=>s.id===state.category);
  state.filtered=state.art.filter(a=>(!style||a.style_id===style.id)&&(!state.search||(a.title+' '+a.category+' '+a.story+' '+a.style_description).toLocaleLowerCase().includes(state.search)));
  const grid=$('gallery');grid.replaceChildren();grid.classList.toggle('gallery-grouped',state.category==='all'&&state.sort==='style');
  for(const c of $('categories').querySelectorAll('.chip')){const active=c.dataset.category===state.category;c.classList.toggle('active',active);c.setAttribute('aria-pressed',String(active))}
  const counter=$('result-count');counter.textContent=state.filtered.length+' '+text('results')+(style?' · '+style.name:' · '+state.styles.length+' styles');
  const description=$('active-style-description');description.textContent=style?style.description:'Browse the collection by artistic approach, from historical painterly techniques to contemporary visual storytelling.';
  $('load-more').hidden=true;
  if(state.category==='all'&&state.sort==='style'){
   // All styles are visible at once. Each group is a curated preview; the button reveals all works in that style.
   for(const entry of state.styles){const works=state.filtered.filter(a=>a.style_id===entry.id);if(!works.length)continue;
    const section=document.createElement('section');section.className='style-group';section.id='style-'+entry.id;section.setAttribute('aria-label',entry.name);
    const heading=document.createElement('div');heading.className='style-group-heading';const h=document.createElement('h3');h.textContent=entry.name;const summary=document.createElement('p');summary.textContent=entry.description;
    const top=document.createElement('div');top.append(h,summary);const open=document.createElement('button');open.className='style-browse';open.textContent='View all '+works.length+' artworks ↗';open.type='button';open.addEventListener('click',()=>setStyle(entry.id));heading.append(top,open);section.appendChild(heading);
    const segment=document.createElement('div');segment.className='style-group-grid gallery-grid';segment.appendChild(cards(state.search?works:works.slice(0,4)));section.appendChild(segment);grid.appendChild(section)}
  }else{
   const sorted=[...state.filtered];if(state.sort==='az')sorted.sort((a,b)=>a.title.localeCompare(b.title));else if(state.sort==='za')sorted.sort((a,b)=>b.title.localeCompare(a.title));else sorted.sort(byStyle);
   const segment=document.createElement('div');segment.className='gallery-grid';segment.appendChild(cards(sorted.slice(0,state.count)));grid.appendChild(segment);$('load-more').hidden=state.count>=sorted.length;
  }
  $('load-more').textContent=text('more');$('no-results').hidden=state.filtered.length!==0;
 }
 function printLinkFor(art){return safeLink(state.print.byArtworkId?.[art.id]||state.print.defaultPrintUrl||art.print_url)}
 function updateBuy(){const btn=$('checkout-button');btn.disabled=!state.checkout||!$('digital-consent').checked;btn.textContent=state.checkout?text('buy'):text('unavailable')}
 function openArt(art,push){state.selected=art;$('dialog-image').src=art.image;$('dialog-image').alt=art.title;$('dialog-title').textContent=art.title;$('dialog-index').textContent='ARTWORK / '+String(art.number).padStart(3,'0');$('dialog-category').textContent=art.category;$('dialog-story').textContent=art.story;$('dialog-edition').textContent=art.edition;$('dialog-size').textContent=art.width+' × '+art.height+' px (public preview source)';$('digital-consent').checked=false;updateBuy();const url=printLinkFor(art);$('print-link').hidden=!url;$('print-placeholder').hidden=!!url;if(url)$('print-link').href=url;
  if(push&&location.pathname!==detailHref(art))history.pushState({art:art.id},'',detailHref(art));
  const dialog=$('art-dialog');if(!dialog.open)dialog.showModal();document.title=art.title+' — Freddy Bremseth Art';}
 function closeArt(){const dialog=$('art-dialog');if(dialog.open)dialog.close()}
 async function beginCheckout(){if(!state.selected)return;if(!$('digital-consent').checked){showToast(text('agree'));return}const btn=$('checkout-button');btn.disabled=true;btn.textContent=state.lang==='no'?'Starter betaling…':'Starting checkout…';try{const res=await fetch('/api/create-checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({artwork_id:state.selected.id,digital_consent:true})});const data=await res.json();if(!res.ok||!safeLink(data.url)||!data.url.startsWith('https://checkout.stripe.com/'))throw Error(data.error||'Checkout unavailable');location.assign(data.url)}catch(e){showToast(text('checkoutError'));updateBuy()}}
 async function verifyPurchase(sessionId){const dialog=$('purchase-dialog');if(!dialog.open)dialog.showModal();$('purchase-status').textContent=text('pending');$('download-link').hidden=true;$('purchase-retry').hidden=true;try{const r=await fetch('/api/confirm-download?session_id='+encodeURIComponent(sessionId),{headers:{'Accept':'application/json'}});const data=await r.json();if(!r.ok){if(data.payment_verified){$('purchase-status').textContent=text('fileMissing')}else $('purchase-status').textContent=data.error||text('failed');$('purchase-retry').hidden=false;return}if(!safeLink(data.download_url))throw new Error('Invalid download URL');$('purchase-title').textContent=data.title||'Your artwork is ready.';$('purchase-status').textContent=text('paid');$('download-link').href=data.download_url;$('download-link').hidden=false;$('download-link').textContent=text('download')}catch{$('purchase-status').textContent=text('failed');$('purchase-retry').hidden=false}}
 async function init(){try{const [catalog,styles,status,print]=await Promise.all([fetch('/assets/catalog.json').then(r=>r.json()),fetch('/assets/styles.json').then(r=>r.json()),fetch('/api/status').then(r=>r.ok?r.json():({sales_enabled:false})).catch(()=>({sales_enabled:false})),fetch('/assets/print-links.json').then(r=>r.ok?r.json():({})).catch(()=>({}))]);state.art=Array.isArray(catalog)?catalog:[];state.styles=Array.isArray(styles)?styles:[];state.checkout=!!status.sales_enabled;state.print=print;makeStyles();$('art-count').textContent=state.art.length;$('end-number').textContent=state.art.length;render();$('year').textContent=new Date().getFullYear();let slug=currentSlug();if(slug){const match=state.art.find(a=>a.id===slug);if(match)openArt(match,false)}const sessionId=new URLSearchParams(location.search).get('session_id');if(sessionId&&/^cs_(test_|live_)?[a-zA-Z0-9_]+$/.test(sessionId)){verifyPurchase(sessionId)}}catch(e){$('gallery').textContent='The gallery could not be loaded. Please refresh this page.';console.error(e)}}
 $('categories').addEventListener('click',event=>{const b=event.target.closest('[data-category]');if(b)setStyle(b.dataset.category)});
 $('art-search').addEventListener('input',e=>{state.search=e.target.value.toLocaleLowerCase().trim();state.count=18;render()});$('art-sort').addEventListener('change',e=>{state.sort=e.target.value;state.count=18;render()});$('load-more').addEventListener('click',()=>{state.count+=18;render()});$('digital-consent').addEventListener('change',updateBuy);$('checkout-button').addEventListener('click',beginCheckout);
 for(const button of document.querySelectorAll('#art-dialog .dialog-close'))button.addEventListener('click',()=>closeArt());$('art-dialog').addEventListener('close',()=>{if(currentSlug()){history.replaceState({},'','/');document.title='Freddy Bremseth Art — Art that stays with you'}});$('art-dialog').addEventListener('click',e=>{if(e.target===$('art-dialog'))closeArt()});$('purchase-close').addEventListener('click',()=>$('purchase-dialog').close());$('purchase-retry').addEventListener('click',()=>{const id=new URLSearchParams(location.search).get('session_id');if(id)verifyPurchase(id)});addEventListener('popstate',()=>{const slug=currentSlug();const art=state.art.find(a=>a.id===slug);if(art)openArt(art,false);else closeArt()});
 init();
})();
