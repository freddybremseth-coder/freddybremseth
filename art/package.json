{
  "name": "freddy-bremseth-art-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "art.freddybremseth.com — curated gallery with verified €50 downloads and preview-only artwork import",
  "scripts": {
    "build": "python3 scripts/import-bulk-previews.py && node scripts/import-kintsugi.mjs && node scripts/english-titles.mjs && python3 scripts/curate-styles.py && node --check assets/js/app.js && node scripts/build-pages.mjs && node scripts/test-site.mjs && node --test tests/*.test.cjs && node scripts/build-public.mjs",
    "test": "node --test tests/*.test.cjs",
    "preview": "node scripts/preview.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "stripe": "^18.5.0"
  },
  "engines": {
    "node": "22.x"
  }
}
