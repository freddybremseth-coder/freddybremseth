{
  "name": "freddy-bremseth-art-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "art.freddybremseth.com — 100-work art gallery and verified €50 Stripe digital downloads",
  "scripts": {
    "build": "python3 scripts/curate-styles.py && node scripts/build-pages.mjs && node scripts/test-site.mjs",
    "test": "node --test tests/*.test.cjs",
    "preview": "node scripts/preview.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "stripe": "^18.5.0"
  },
  "engines": {
    "node": "22.x"
  }
}
